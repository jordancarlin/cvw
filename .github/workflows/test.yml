on: push
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: |
          df -h
          ./.github/cli-space-cleanup.sh
          df -h
      - run: |
          ls -l
          du -h --max-depth=1
